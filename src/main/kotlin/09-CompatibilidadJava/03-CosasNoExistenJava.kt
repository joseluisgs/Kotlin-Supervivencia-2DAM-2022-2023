package `09-CompatibilidadJava`

/**
 * Tenemos determinadas cosas que no se pueden usar en Java
 * Tipos reified (reified)
 *
 * Corrutinas y suspend (coroutines). Podemos usar corrutinas en segundo plano y devolver
 * un resultado cuando se haya terminado compatible en Java
 *
 * Compose e interfaces declarativas
 */